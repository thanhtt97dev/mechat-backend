version: '3.9'
services:
  redis:
    container_name: me-chat-redis
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 60 1 --loglevel warning --requirepass mechat
    volumes: 
      - ./data:/data